create table student_subject(
    id number generated by default as identity primary key not null,
    student_id number not null references student(id),
    subject_id number not null references subject(id)
);